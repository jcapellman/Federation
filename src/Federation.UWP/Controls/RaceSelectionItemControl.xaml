<UserControl
    x:Class="Federation.UWP.Controls.RaceSelectionItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Federation.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ToggleButton x:Name="tbMain" Margin="0,10,10,10">
        <ToggleButton.Background>
            <SolidColorBrush Opacity="0.5" Color="Black" />
        </ToggleButton.Background>

        <ToggleButton.Content>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Image Grid.Row="0" x:Name="imgRace" />
                <TextBlock Grid.Row="1" x:Name="tbName" HorizontalAlignment="Center" Margin="0,0,0,10" Style="{StaticResource HeaderTextBlockStyle}" />
            </Grid>
        </ToggleButton.Content>
    </ToggleButton>
</UserControl>